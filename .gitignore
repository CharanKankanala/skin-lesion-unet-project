venv/
__pycache__/
*.pyc

# Big artifacts (do NOT push)
data/
*.pth
outputs/checkpoints/

# Inference cache + outputs (downloaded models, generated images)
models_cache/
inference_outputs/

# Skip raw images by default (none should be in the repo anyway)
data/raw/**/*.jpg

# Keep figures: re-allow PNGs only inside outputs/figures/
*.png
!outputs/figures/*.png

.DS_Store